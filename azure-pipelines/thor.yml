trigger: 
- master

variables:
- group: FwaStatsTracker-Thor

extends:
  template: template.yml
  parameters:
    environmentName: $(EnvironmentName)
    architecture: $(Architecture)
    selfContained: $(SelfContained)
    destinationFolder: $(DestinationFolder)